{
  "name": "@abs/api",
  "version": "0.1.0",
  "private": true,
  "description": "Ambulance Booking System API (NestJS)",
  "scripts": {
    "start": "node dist/main.js",
    "dev": "nest start --watch",
    "postinstall": "prisma generate",
    "prebuild": "npm run prisma:generate && npm run build --workspace @abs/contracts && npm run build --workspace @abs/config",
    "prisma:generate": "prisma generate",
    "build": "nest build",
    "test": "jest",
    "test:watch": "jest --watch",
    "seed": "node scripts/with-env.mjs ts-node prisma/seed.ts",
    "db:push": "node scripts/with-env.mjs prisma db push",
    "migrate": "node scripts/with-env.mjs prisma migrate dev",
    "db:deploy": "npm run db:push && npm run seed"
  },
  "dependencies": {
    "@abs/config": "*",
    "@abs/contracts": "*",
    "@nestjs/common": "^10",
    "@nestjs/core": "^10",
    "@nestjs/jwt": "^10",
    "@nestjs/platform-express": "^10",
    "@prisma/client": "^5.22.0",
    "@types/supertest": "^6.0.3",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.6.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^10",
    "@nestjs/testing": "^10",
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "prisma": "^5.22.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.2"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
