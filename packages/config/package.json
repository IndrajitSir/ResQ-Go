{
  "name": "@abs/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration helpers (env parsing, ports, constants)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
